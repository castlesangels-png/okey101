class OkeyTileVisualSpec {
  const OkeyTileVisualSpec({
    required this.label,
    required this.numberText,
    required this.colorKey,
    required this.isOkey,
    required this.isFakeOkey,
  });

  final String label;
  final String numberText;
  final String colorKey;
  final bool isOkey;
  final bool isFakeOkey;
}
